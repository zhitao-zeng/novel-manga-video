# 雾月：对白切分恢复与资产检查提速（2026-09-14）

## 修改

- `build_clip_plan_thin.shots_for_plan` 支持选择片段，但始终用整集的兄弟片段计算旧切分位置。无关片段坏数据不阻断所选片段。
- `repair_clips_thin` 重建时传完整计划；按计划中的 prepared shot index 寻址，保留原始 `origin_index` 溯源字段。
- `repair_split_ranges` 恢复旧计划中重复 source index 的片段，也检查共享原始分镜但被回填成超时长的片段（索引本身不一定重复）。保留片段 ID、顺序和未涉及片段；重编译涉及片段，移除旧英文请求，通过正常翻译、渲染、复审流程处理。无法还原旧边界时保留并报告，不自动重新打包整集。
- `thin_batch` 在生成任务的英文提示词准备前调用恢复函数。缓存重组、只复审、dry-run 不改计划。
- `render_clips_thin` 的清理、等待重画、解码校验都限定为本集实际引用图片；不再遍历、解码全书图片和无关备份。fast 不读取或制作表情卡。质量模式保留原有第二视图建卡行为。

资产已有 `series_assets/manifest.json` 索引。瓶颈是检索路径后仍解码全库，并非缺少数据库。本次没有引入新的数据库或校验缓存。

## 验证

- 新增 `tests/test_split_repair_and_assets.py`：10 项通过。覆盖旧切分定点重建、源编号不同、两种历史回填错误的真实写回及再次运行、不能还原时保持原数据、翻译前恢复、只检查所需资产及坏卡重建、不删除缺失图片的在途任务记录、质量模式的阶段角色卡无需凭空补表情卡。
- `test_review5_fixes`、`test_single_card_plan`、`test_lane_repairs` 回归通过。
- 扩展运行 `test_split_long_stages`、`test_sd_repairs`、`test_review3_fixes`、`test_review4_fixes`：51 通过，1 条已有失败。`test_a_correction_goes_into_the_english_prompt_in_english` 仍断言旧 `director_note` 字段；当前已提交的翻译器把导演指令放入 `summary`。翻译器和该测试与 HEAD 均无差异，本次未改动。
- 第 1316 章 clips 05–08：从每段都含 177 字，恢复到 28 / 53 / 51 / 45 字；请求时长 9 / 15 / 15 / 15 秒。合并后的对白与原始分镜对白逐字相等，恰好一遍，其余片段逐字段不变。
- 第 569 章 5 张实际引用图，纯解码校验 0.359 秒。
- 生产日志：第 1216 章 23:18:01–23:18:02、第 1224 章 23:18:07–23:18:09，资产准备约 1 / 2 秒。此前抽样 28 次资产准备均值约 112 秒。前者是准备阶段时间，不是整集总耗时，也不代表 H3 推理加速了同样倍数。

## 生产恢复

运行中的数据一直变化：初筛 221 集 550 段，随后预检时为 219 集 546 段（期间正常修复任务也在写回）。预检可恢复 115 集 357 段；另 189 段边界不一致或缺少兄弟片段，不能用本次“保持既有切分”的办法恢复，保留在清单里。

补查共享分镜但索引不重复的超长片段，共 53 段，其中 14 段可按现有记录恢复；另 39 段同样无法保持既有边界完成恢复。两次预检合计 371 段可恢复、228 段需要继续处理旧切分/时长问题。

写回时只短暂暂停统一管理器的新调度，让已有子任务继续；避开其占用章节，待任务结束后补写。23:40 已完成全部可直接恢复的 **115 集 371 段**，含第 1316、595、1261、1929、2017 章，待占用任务释放的清单已清空。管理器已经恢复运行。改了请求的章节成为 stale，由统一补渲、复审队列处理；恢复计划不等于成片已合格。23:38 抽查时，首批已有 12 集技术完成、1 集完成但有警告；其中两集之后又补写了额外切分修复，会再通过队列处理。

每集首次修改前保留 `clip_plan.json.bak-split-ranges`。脚本不删已有视频，不另启重叠修复线。详细运行记录：

- `outputs/wuyue/repair_manager/split-range-preview-20260914.json`
- `outputs/wuyue/repair_manager/split-range-extra-preview-20260914.json`
- `outputs/wuyue/repair_manager/split-range-recovery-20260914.json`

代码尚未提交、推送。本次在现有工作树上做最小修改，其他会话的未提交工作仍保留。
