# 星海龙途 H3 片段精判

2026-09-15 16:37 开始，16:40 左右由 4 并发提高到 8 并发；按用户最新要求，16:53 已收窄为只审 H3。输入目录为 `outputs/xinghai`。

## 范围与资源

- 当前队列为 3,836 个已有 H3 片段，分布在 376 集。按所选视频旁的 `clip.mp4.task.json` 中实际生成模型筛选，不按章节区间或计划模型推断。
- SD2.5 的 5,131 段和 SD2.0 的 1,714 段已退出本轮。此前完成的 81 条 H3 结果继续复用。
- 初始全书统计为 10,730 个计划片段，其中 49 段缺文件；该缺失数是全书口径，不并入当前 H3 待审数。
- H3 队列包含旧判官标错的片段，按本轮精判重新核对。
- 使用现有 7 个 Qwen3.8-27B 服务入口，启动前后采样均没有请求排队。雾月继续使用原来的修复总控。
- 本次任务范围为审查现有视频并更新审查记录，未启动星海的自动修复或视频生成。

## 题材适配与试审

已有 `review_normal.txt` 原先供第二道审片使用，这次也接入精判。星海允许拟人化龙、直立和手掌、幼龙到巨龙的体型差别、正常群像以及本来相似的白龙。

具体段落原文、当前成长阶段和明确身份优先于笼统规则；世界观说明不能免除动作或台词安错人、缺失必要演员、超出原文人数的复制等问题。

限制模型前，试审第 1、2、59、706 集各一段，3 段通过，59 集一段被判角色前后形态不一致；四段都没有因正常拟人龙被标为物种错误。这四条实际来自 SD，现已随全模型结果归档，不计入 H3 队列。

## 持续运行与恢复

复用 `scripts/shared_audit_thin.py` 的独占领取、当前视频匹配和续跑机制。主 Qwen 结果逐步写回 `episode_review.json`，只覆盖本轮已审的 H3 片段；首次覆盖前的旧报告保存在 `repair_manager/before_reviews/`。视频文件保持原样。

切换范围时，原全模型队列归档为 `audit_all_models_before_h3.sqlite3`，当时的章节结果保存在 `all_models_review_results/`。已恢复非 H3 片段原审查记录，并验证无非 H3 片段混入当前审查范围。

状态和恢复资料位于 `outputs/xinghai/repair_manager/`：

- `audit_launch.json`：当前 PID、并发、输入范围。
- `audit_status_qwen.json`：持续更新的进度。
- `shared_audit.sqlite3`：每段任务与结果，断点续跑依据。
- `audit_h3_scope.json`：H3 来源筛选与当前范围。
- `audit_qwen.log`：运行日志。
- `audit-tests.xml`：33 项相关测试通过，覆盖书本规则、只审片回写、原报告备份、任务独占和当前素材判定。
- `audit-h3-scope-tests.xml`：23 项相关测试通过，补充验证局部审查保留非目标模型记录。

正常停止会先完成在途请求；重新运行同一队列可以接续。审查完成仍需区分视觉结论、缺失素材和媒体技术状态，不能单凭审片结束宣布全书交付合格。

本轮代码与记录未提交或推送。
