# 诸天故障修复与恢复运行（2026-09-16）

本次承接 `outputs/zhutian-diagnosis-20260916/问题诊断.md` 中的六类问题。用户随后明确表示不处理 2661–2670 的坏原文；这十章保留 `needs_source`，不再自动重试或猜测还原。

## 实现

- H3 使用独立的直连客户端提交、轮询、下载；公网图片服务继续使用原客户端。轮询或恢复遇到 5xx，与连接失败一样冷却旧实例并转交池内有效实例。排空中仍有效的任务可以继续等待或下载，不因停止接新单而重复提交。
- 身份读取先判断原文可读性。原始模型结果保存在章节 `identity_source_reading.json`。无效辅助称谓单独丢弃并记录，其他有效主体保留；完全没有有效指称的主体只修正自身证据一次，仍无法确认就明确失败。没有关闭原文证据校验。
- 原文主体区分个体、群体和道具。群体通过现有 `crowd_roles` 表达，数量未明确时使用“若干”，不把群体绑定成一个 Subject 的重复副本；英文编译重试包含上一次具体冲突。
- 将经过原文核对的道具类型放入本书 `entity/types.json`。规划候选和打包人物引用排除这些道具，保留事件中的器物行为。道具仍绑定为人物时，生成前阻断。模型临时把坐骑识别成 object，不足以自动移除其角色卡。
- 原文已确认的类型错误与旧卡片审查意见分开：旧审查使用的人物性别、衣色、昼夜等设计资料也可能有错，不直接升级成生产硬门。联调中已撤回仅按旧卡片 `regenerate` 字段阻断的处理，解除它临时影响的 2322、2396；这不影响炼妖壶类型错误的硬阻断。
- 局部预审问题按已定位的 stage 修复。只有 `stage=0` 的真实缺失事件进入整章重规划；地点也可在局部修复中更新，重新打包采用修正后的地点。修后仍需要预审通过。
- 预处理异常和未通过的局部修复可以有界重试，每轮输入/策略最多三次，带退避；不可读原文不进入重试。开始新轮时清除旧错误/审查字段，避免旧报告残留伪装成本轮结论。
- 预处理尊重生产已认领的章节，记为 `production_owned`，避免准备脚本与生产工作进程同时编辑计划。
- 旧 30 秒计划进入 H3 准备时按 15 秒重新打包；打包子进程显式传入 H3 时长，防止默认值回到其他产线。

## 运行恢复

H3 运维范围仅涉及本项目客户端与任务；没有停止或重新占用夜班归还后的其他用户资源，也没有重启远端模型服务。

23 集因服务故障停下的任务已恢复派发。只有这些任务的旧整集执行计数因服务故障被重置，备份留在修复证据目录；段级实际生成历史保留。2172 的 30 秒旧计划已重新打包为六段、单段不超过 15 秒，并交还准备队列核验。

52 集已加入 `entities` 定向修复流程（包含 2269 实测）：只修改涉及器物身份的片段，其余保留缓存；计划在完整提案通过后写入，继续走生成、精判和发布。若历史段级预算已用尽，只在原文核验后确有实质请求改动时增加一次生成额度，历史不清零。

## 验证

- 708 项本地测试通过。包括新任务与续跑任务遇到 502 的转交、H3 专用客户端、无效辅助指称隔离、群体表达、道具阻断、局部修复范围、有界重试、生产与准备所有权、未完成提案不发布。
- 2052、2158、2875、2060、2062、2269 六章身份实测成功；这是身份准备验证，不代表六章成片均已通过。
- 2060、2062 的 `clip_01` 从单一角色冲突转为群体表达，英文编译通过，冲突检查为空。未为这两个编译样本额外生成视频。
- 2328：原先卡住的 `clip_03` 经有效实例 28 秒生成；整集 75.776 秒合成并通过技术检查。后续内容复审仍发现另一个缓存片段有多余人物，进入正常内容修复；不能把技术恢复当作全章内容通过。
- 2269：`clip_04` 去掉错误的炼妖壶人物绑定并改写局部画面，随后生成、合成，11 段精判全部通过。
- 11:10:41 总控快照：可交付 206 / 1848，执行挂起 0；实体定向修复仍在运行，准备队列继续处理旧问题。不是全书完成。

运行证据位于 `outputs/zhutian-fix-20260916/`：`tests.xml`、`status-after.json`、`source-backups.json`、`identity-pilot-*.json`、`group-pilot-*.json`、任务状态及计数变更前的备份。

生产总控和预处理均已恢复，星海 SD 暂停策略未变。代码在 `feat/fast-tier` 工作树，本次未 commit、未 push；工作树还保留其他任务之前的未提交改动。
