# 雾月停滞修复与恢复运行

2026-09-15，`feat/fast-tier` 工作树；本轮未提交、未推送。

## 起因与修复

18:22 检查时可交付为 1970 / 2043，自 17:51 起没有增加。总控仍显示 running，却每约 48 秒重新为 1689 / clip_11 增加一次 repair_take；该段计划 16 秒、请求 15 秒，始终在生成前被拦截。69 个段级阻塞主要发生在准备阶段。

- **结构问题先修结构。** 调度不再把时长和拆段冲突作为换种子重拍；统一准备入口先恢复源阶段地址，检查旧 part 编号能否从当前剧本重建。必要时只重排有关联的连续片段，保留台词顺序和其他片段。新拆出来的片段也进入实际生成记录。1689 的超时段拆为 clip_11 和 clip_22；359、1696 的旧切分记录也已恢复。
- **分镜不变仍可复审旧画面。** 原文核验后的分镜没有差异，不再被误当成准备失败。重复错误分流到 reframe 后若没有改动，也转入原文复核，由真实画面决定保留还是重拍。
- **引用直接从原文取。** 台词归属模型可以返回原文段落编号，程序摘取连续原文；抄错字、擅加省略号的回答只补正未通过的条目。归属核对读取全章，分镜重写仍使用目标片段和相邻段落。原文的“莪/伱”与改编的“我/你”仅在台词对应时归一，保存的原文引用保持原样。
- **区分原话与有依据的对白改编。** 支持原文叙述的事实转为在场人的简短对白，以及共同发言由其中一人代表；记录 relation，不冒充原著逐字归属。无依据的承诺、身份或事实仍被拦截。改变叙述改编的主体须有原文明写的人物依据。
- **声音事件保留。** 猫叫等已由打包器转为声音事件，不再被误报为删除了对白；真正的台词仍检查文字是否被改变。
- **主卡与身份纠错。** 实际查看了 1687 的议长主卡，确为灰发男性，与原文金发女性矛盾。新增 1687 专用议长主卡；原文的“知识与智慧的大秘女奥黛拉·赫尔曼”新增独立女性条目与主卡，移除该集对男性“赫尔曼”的错误绑定。两张均为主角色单人卡，不是表情卡。议长使用已有 phases 机制限定在 1687，旧基础图片保留。
- **重拍次数保留。** 默认仍是每段 3 个实际新视频。仅针对 204/05、1843/12、1687/08 的具体修正，记录有原因的一次额外生成机会，上限各为 4；派单和渲染读取同一份 `repair_budget_grants.json`。没有清零历史，也没有全书抬高上限。

相关代码：`manage_repair_thin.py`、`managed_repair_thin.py`、`repair_blocked_plan.py`、`repair_clips_thin.py`、`source_recheck_thin.py`、`prepare_recovery_thin.py`、`render_clips_thin.py`、`h3_request_checks.py`。

## 生产证据

18:35 恢复总控。18:49:33 快照可交付 **1990 / 2043**，比停滞时净增 **20 集**。18:52:08 已到 **1995 / 2043**，净增 **25 集**，剩 **48 集**；这不是剩余章节全部修完。

- 359、1469、204、1843 已整集通过。
- 1984 / clip_02 按原文复核旧视频通过并获准复用；当时整集仍有技术收尾，不能把段通过写成整集通过。
- 1689 已实际生成并复审，随后按新的画面问题继续修复，没有再陷入原来的 16/15 秒空转。
- 1687 已使用两张正确女性主卡生成，18:49 快照正在复审，尚未宣布整集通过。
- 754、1722 的“莪/伱”问题已用真实章节再次核验，两集的说话者均成功确定，事实记录已保存并接回总控。

总控状态仍在 `outputs/wuyue/repair_manager/state.json`。最后一次接续保留在途子进程，由新总控继续接管；没有改动 GPU 服务或星海的审查任务。看板服务已重载新口径，API 与总控在 18:49 同报 1990。

本轮记录位于 `outputs/wuyue/repair_manager/unblock-20260915/`：原状态、阻塞清单、真实小样、拆段报告、资产绑定记录、各次接续记录和测试结果。后续进度以总控实时状态为准。

## 验证与资产

相关回归 **157 项通过，0 失败、0 跳过**。覆盖时长冲突不再走重拍循环、相邻原文、段落编号引用、错字对应、无改动复审、对白保护、群体卡绑定、段级预算，以及既有拆段、发布和看板逻辑。测试报告为 `outputs/wuyue/repair_manager/unblock-20260915/tests-final.xml`。本地测试之外，上述生产结果分别来自实际生成、实际审查和实际发布。

两张图通过内置 image_gen 生成，已查看输出并存入项目。PNG 原图保留，仅做 JPEG 格式转换供产线读取：

- `outputs/wuyue/series_assets/characters/character_127-wy1687/turnaround.jpeg`
- `outputs/wuyue/series_assets/characters/character_323/turnaround.jpeg`

完整生成提示词：`outputs/wuyue/repair_manager/unblock-20260915/image-prompts.json`。图片和生产数据保持在输出目录，不加入 Git。
