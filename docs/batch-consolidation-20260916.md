# 批量产线整理 · 2026-09-16

正式生产统一为现有批量产线；前端看板保留。

## 三个提交的职责

1. 删除旧生成 HTTP API、旧命令、旧整集控制器、专用导出验证功能和 API 容器。把初始人物库生成提取到共享库，移除六个旧 API 专用依赖包。
2. 把身份、动作和对白归属提取为共享规则；规划、修段、打包使用同一实现。17 个实验脚本及早期完整规划器移入实验目录。物件继续可以作为动作目标，但不作为人物卡参与选角。
3. 给原管理入口增加生产、准备、修复三种流程；复用原执行器及其状态、锁和重试记录。看板与命令行使用相同状态读取逻辑，区分暂停、在途收尾、进程停止和完成。

共用规则以整理开始时工作树里已验证的修复为基线。身份、修复与准备队列、看板所需的既有改动随对应阶段纳入；资源配置、视频供应商及音视频质检等无关改动保留在原工作树，不整包提交。

## 验证

- 从实际暂存内容导出独立目录，完整测试 **760 项通过**，避免未提交代码补齐依赖导致误判。
- 盛唐 1、诸天 2130 / 2340、雾月 422、星海 1：冻结 5 章、25 段，整理前后的打包计划和提示词逐项一致。
- 动作回归包含临时动物、物件、空目标、合法自我动作及画外目标；对白测试保留拆分后的说话人归属。
- 模拟验证重复启动、暂停续跑、在途收尾、原重试记录保留，以及看板和 CLI 状态一致。
- 依赖锁文件校验通过；批量模块与看板在禁止导入旧 Web 依赖的子进程中正常导入。

本次未调用真实图像、视频或文本生成服务，没有更新历史剧本、人物库或视频，也没有恢复诸天生产。

当前操作以根目录 README 为准。日期文档是历史记录；原文重建仍在 experiments 中，未接管生产。
