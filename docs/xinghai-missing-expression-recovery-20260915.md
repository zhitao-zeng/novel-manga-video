# 星海 H3 的旧表情卡依赖解堵

2026-09-15 检查修复进度时发现：21:11 可交付为 196 / 376 集，另有 21 集被生成前准入挡住。逐项读取原因后，全部指向已不存在的 `expressions.jpeg`，不是普通的分镜结构问题。

此前为了避免重渲已审合格的缓存片段，批渲染保留了这些旧片段的参考列表。但当列表中的旧表情卡文件已不存在时，资产准入仍会挡住整个片段；新目标队列优先派发，又使这些等待中的章节迟迟无法获得恢复步骤。

本次处理：

- `single_card_plan.repair_missing_expressions` 只清理缺失表情卡的依赖，保留主卡及原来的事件、台词、切分；重新整理图片编号并编译对应的英文请求。
- 对有当前 take 精判通过记录、没有技术或待确认问题的旧视频，使用已有素材接纳机制保留它；这是复用已有审查结论，不伪造一次新的审查。原视频、笔记或剩余参考图变化后，该接纳会失效。
- 已知有错或未审过的视频不会因此获准复用，继续经过正常生成、技术检查及内容复审。
- 清理写入修复历史。若别的片段已有尚未渲染的准备记录，同步其最终计划标识，避免后续实际生成漏记次数。
- 总控新增 references 恢复步骤，优先接管这类被旧引用阻挡的章节，再领取更多新目标；旧等待任务移交后标为 superseded，仍共用原有 24 集修复容量。
- 批渲染入口也接入该清理，防止下一批再因相同缺失文件停住。已有且有效的非目标片段不变，不补画表情卡。

相关测试覆盖：清理后图片编号和剧情不变；只复用当前已通过的视频；错误视频不能被接纳；视频换版后接纳失效；待渲染记录的计数衔接；旧任务移交与优先派发；既有范围、缓存、拆段、复审和发布逻辑。结果在 `outputs/xinghai/repair_manager/missing-expression-tests.xml`。

21:28 已接续新总控，原在途子任务保留。第 489 集完成引用清理并返回合成，第 495、498 集进入引用清理。逐集处理结果保存为 `missing_expression_repair.json`，总控接续记录在 `missing-expression-rollout.json`。SD 审查与雾月 ModelScope 上传未停止。

本轮未提交或推送代码；是否整集通过仍以当前成片、复审和发布结果为准。
