# 全部重构实施记录

实施分支：`codex/full-pipeline-consolidation`，工作树：`/mnt/disk1/zengzhitao/novel-manga-video-full`。
起点：`0c02ac9`。原运行目录的 H3 提示词、ASR 检测和期间新增的提示词修改均已接入；内容保留在对应的新模块中。诸天继续暂停。

| 单元 | 状态 | 证据 |
|---|---|---|
| C01 基线 | 完成 | 894 项原测试通过；五章 25 段、两条请求路径冻结；F01–F04/F06 严格预期失败用例 |
| C02–C03 H3 | 完成 | 新声明句式通过；表达/编号/校验集中到 story/h3；25 段对照 |
| C04–C05 对白和音频 | 完成 | 改词同步派生字段；识别与评价分开；无对白 ASR 四种场景调用不变 |
| C06–C07 画幅 | 完成 | Seedream 横屏修复；移除 HTTP 方法补丁；人物/场景及并行请求对照 |
| C08–C09 章节资料 | 完成 | 身体卡每次操作刷新；派单扫描复用章节输入；写回后下一轮读取新值 |
| C10 配置 | 完成 | 三本书有效配置对照；新书只需登记配置；端点与时长选项按调用隔离 |
| C11 文本调用 | 完成 | llm 配置/传输/响应分开；初始化、规划与审查共用；78 项相关回归 |
| C12 规划规则 | 完成 | 归一化、原文、构图、时长分开；五章 15/30 秒共十组前后完全一致 |
| C13 统计 | 完成 | 历史/当前、本地/付费分开；已有任务去重；记录缺失和价格覆盖；24 项回归 |
| C14 模型与历史依赖 | 完成 | 现用类型按领域分组，六个持久化根模型 schema 不变；32 个类型和实验专用模块隔离；107 项回归 |
| C15–C16 应用归包 | 完成 | 执行/调度/看板均入 application；内部不导入命令；移除媒体和编译纯转发接口 |
| C17 看板 | 完成 | HTML/CSS/JS 独立；10 项资源入包；真实浏览器前后实时/看板四页显示进度、柱状图、暂停状态 |
| C18 测试文档与落地 | 完成 | support 夹具集中；无跨 test_ 导入；最终提交导出和运行目录各 933 项通过；已合入 main 并重载必要服务 |

基线日志及另一任务起始文件副本在 `outputs/full-refactor-20260917/`，不进入 Git。
冻结请求覆盖 H3 表达、SD 请求、对白绑定、缓存匹配、修复路由与翻译约束；规划、修段、字幕、历史及状态轨迹复用原有相关回归。
冻结文件不是模型效果验证；全部模型响应模拟。无对白 ASR 检测不因本轮整理被关闭。

用户已确认采用运行工作树中落定的提示词与检测版本，接入提交 `1c0473e`；独立工作树开发期间未修改原运行文件。原文提示词、字幕绑定、缓存对照只允许确认的 H3 文本变化。
第一批完整测试：911 passed、1 xfailed；唯一预期失败是尚待 C13 修复的费用统计。日志：`outputs/full-refactor-20260917/batch-one-final-tests.log`。
原有供应商测试绕过构造函数的夹具已显式补全画幅参数，保留原请求与重试断言；测速实验的常量导入已跟随语音规则迁移。

旧接管核对结果在 `outputs/full-refactor-20260917/legacy-use.json`：雾月已接管任务 done/superseded，星海和诸天无旧接管任务，旧控制脚本进程为 0。仅退休一次性接管入口；原生队列、暂停、历史和已接管记录的读取未删除。
`models/runtime.py` 保留历史视频质检/检查页仍使用的计划类型；`models/drama.py` 保留当前 EpisodePlan 字段引用的类型，未因旧名称而删字段。

运行目录落地前又核对到提示词新增角色辨识特征、含表演说明的说话人提取；分别接入 `cb08472`、`6a9df52`。新结构保留这些修改，并验证原有结构化 25 段请求与缓存决定不变。每次合并前均按文件内容核对，没有覆盖未接入的其他修改。
正式包 261 个模块在不含 scripts/experiments 的导入路径下可加载，导入期间禁止写生产记录和请求模型；两个原先在导入时执行的资产操作已改为显式命令。
发布包离线构建复用了本机 UV 缓存中的构建依赖，没有修改现有 Python 环境或下载模型。浏览器截图和发布包核对材料在同一 outputs 证据目录中。

## 最终验收与运行状态

- 已验证代码版本：`6a9df52`；其后的交付记录只改文档。
- 该提交的干净导出：933 passed；实际 main 运行目录：933 passed，无预期失败或跳过。
- 真实浏览器验证前后实时页和看板页：进度条、柱状图、暂停状态正常；新增用量明细可展开。
- 最终发布包包含 10 项页面资源；261 个模块在没有 scripts/experiments 导入路径时可加载，导入不写生产数据、不请求模型。
- main 已快进到重构成果。仅重载暂停中的诸天管理器及看板子进程，保留原看板守护进程。
- 诸天 pause 内容、任务步骤/状态、预算相关计数、处理轮次、章节范围与合并前一致；管理器状态 paused，在途 0。
- 实际 `/`、`/board`、静态脚本、`/runtime.json`、`/board.json` 均返回 200；三本书均已加载新用量统计，history_error 为空。
- 没有启动规划、生成、重拍或历史补查；本轮未推送远端。

最终证据以 `outputs/full-refactor-20260917/final-export-tests.log`、`runtime-tests.log`、`completion.json`、`wheel-smoke.json`、`browser/results.json` 为准；较早日志记录了实施中已解决的问题。

## 主要提交对应

| 单元 | 提交 |
|---|---|
| C01 基线 | `25a17b2` |
| C02–C03 H3 | `a18b19e`、`262023d` |
| C04–C05 对白与音频 | `c672366`、`8a62a10` |
| C06–C07 画幅 | `1a29bb1`、`8ec1015` |
| C08–C09 章节资料 | `027b1b8`、`285f0a8` |
| C10 配置 | `19dd09d`、`38cc5f9` |
| C11 文本模型 | `f0765ed` |
| C12 规划职责 | `48fdce8` |
| C13 统计 | `77ec125`、`ceba808` |
| C14 模型/实验 | `0ceb71d` |
| C15–C16 归包 | `f4277c3`、`acb7597`、`5567306`、`b86fa27` |
| C17 前端 | `8a104b7` |
| C18 夹具/文档 | `5cea008` |
| 接入其他任务的确定修改 | `1c0473e`、`cb08472`、`6a9df52` |

修复、夹具跟进和交付记录分别提交，因此实际提交数多于原先的 18 个逻辑单元；范围仍是 R01–R12。
