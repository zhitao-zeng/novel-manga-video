# 实时页加载修复（2026-09-16）

此前 `/status.json` 在请求内逐集读取三本书的旧计划、质检和审查报告，同时等待 H3 实例统计。服务重启后的缓存为空，实时页必须等完整扫描，实际请求出现 6 秒及 40 秒超时。上一轮仅拆出了进程/在途接口，主实时接口仍有这条慢路径。

本次改动：

- 实时页使用总控已保存的当前进度，保留 15 秒刷新；不在 HTTP 请求内重算全书历史。
- 历史制作数据从后台看板缓存补充；未读取的历史数字显示待加载，不伪造为零。
- H3 服务统计使用直连的后台读取，不阻塞主页面；首个请求先显示已有生产状态。
- 服务启动不自动扫描历史全书，打开历史看板时才启动后台扫描。
- 后台监视器先发布轻量的交付和资源采样，再按原周期收集详细修复记录。

验证：41 项相关回归通过，新增冷缓存响应和稀疏历史数据的前端渲染检查。2026-09-16 11:38:54 本机 Chromium 实测，服务重启后首次显示三本书当前进度耗时 **1.398 秒**；随后状态接口 **129 毫秒**、页面刷新显示 **87 毫秒**，没有浏览器脚本错误。这是本机样本，不包含用户访问链路的延迟。

只重载看板服务。诸天生产和预处理保持运行；本次未 commit、未 push。
