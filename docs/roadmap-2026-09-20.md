# 项目路线图（2026-09-20）

本文按"什么挡住了交付"排期，不按功能清单排期。每条都给可验证的完成标准。
现状数字来自 `outputs/<novel>/delivery.json`（诸天为 09-20 11:00 重算，雾月 09-15，星海 09-15）。
遗留问题引自 [H3 静音修复](h3-silent-speech-repair-20260918.md)、[分场与导演交接](scene-method-pipeline-2026-09-18.md)。

## 0. 基线

| 小说 | 总集 | 可交付 | 占比 | 技术挡 | 审查挡 | 剧本影子门（标记/会挡） |
|---|---:|---:|---:|---:|---:|---:|
| 雾月秘典 | 2043 | 2042 | 100.0% | 0 | 1 | 758 / 757 |
| 星海龙途 | 1600 | 532 | 33.2% | 412 | 656 | 142 / 41 |
| 诸天万象录 | 3841 | 263 | 6.8% | 2446 | 1132 | 381 / 19 |
| **合计** | **7484** | **2837** | **37.9%** | | | |

三本书的性质不同，不能用同一套动作：

- **雾月**：量已经做完，欠的是质量债（757 集有幻觉人物，画内台词嘴不动）。
- **星海**：一半卡在上游没跑完（技术 pending 397、审查 not_ready 407），一半卡在审查判定（must_fix 147 集 / 223 段）。
- **诸天**：技术 pending 2369，即"大部分还没渲染"。这是产能问题，不是质量问题。

## P0 — 复产前必须解除的阻塞

诸天一旦恢复调度就会撞上这些。不先解决，恢复即返工。

| # | 事项 | 现状 | 完成标准 |
|---|---|---|---|
| 0.1 | **H3 池按时长派活** | 两卡实例跑 >14 秒 768p 会显存不足并拖死实例。当前只在修复脚本里绕开，`providers/local_h3.py` 的池子仍不按时长分配 | 池子按段时长选实例规格；构造一个 >14 秒段验证不再拖死实例 |
| 0.2 | **修复管理器的 legacy-dir 已不存在** | 运行中的 `manage_repair_thin.py` 启动参数指向 `/mnt/disk1/zengzhitao/tmp/zhutian-h3`，该路径已被删除。当前 `status: paused` 所以没报错 | 确认该参数是否仍需要；不需要则去掉，需要则恢复目录 |
| 0.3 | **代码单点** | 34 个 commit 领先 origin/main，另有 27 个改动文件、26 个未跟踪文件未提交 | 提交并 push，`git status` 干净 |
| 0.4 | **星海 5 集预算锁** | 54、258、350、1092、1171 的问题段各生成过 3 次，被 `MAX_GENERATED_TAKES = 3` 挡住，产物已不在盘上 | 通过 `repair_budget_grants.json`（须写 `reason`）放行，5 集重拍落位 |
| 0.5 | **26 集 cache_miss** | `prompt_h3` 与已渲染段对不上（旧格式提示词带中文人名，或段级修复改了分镜没重渲） | 对齐后重拼，26 集进入正常状态 |

## P1 — 把已有产能变成交付

这一阶段不改质量标准，只把已经做了一半的推完。

| # | 事项 | 量 | 说明 |
|---|---|---:|---|
| 1.1 | 诸天技术渲染 | 2369 集 pending | 纯调度问题。P0 完成后恢复 `production` 流程 |
| 1.2 | 星海技术收尾 | 397 pending + 9 clips_failed + 1 stale | clips_failed 要逐个看原因，不能重跑了事 |
| 1.3 | 审查积压 | 诸天 1045 + 星海 585 pending | 审查是模型调用，可与渲染并行 |
| 1.4 | must_fix 修复 | 诸天 934 段 / 星海 223 段 | 走现有 `repair` 流程 |
| 1.5 | 诸天 32 集 stale + 2 集 no_plan | 34 集 | stale 说明计划与产物不同步 |

**阶段目标**：星海 ≥90%，诸天 ≥60%。合计可交付从 2837 升到约 4000+。

## P2 — 质量债

这些不影响"能不能交"，影响"交出去像不像样"。

| # | 事项 | 量 | 决策点 |
|---|---|---:|---|
| 2.1 | **口型存量** | 雾月+星海 12022 段 | 画内台词写成画外音、嘴不动。静音修复只对新内容生效。全量重生成的成本需要先估 |
| 2.2 | **剧本影子门转正式门** | 雾月 757 集会被挡 | 现在是 shadow 只标记不阻断。一旦转正，雾月交付率从 100% 掉到 63%。**转不转是产品决策** |
| 2.3 | 幻觉人物榜首 | 雾月「薇奥拉公主」215 次、「猫咪」90 次；诸天「模糊身影」27 次 | 是身份解析问题还是原文就有，要分开 |
| 2.4 | `flow_shift` 参数 | 全局 | 仍是 4 步时代的 12，官方推荐 6。对现有内容无差别，快速运动的书需要重测 |

## P3 — 能力建设

| # | 事项 | 现状 | 完成标准 |
|---|---|---|---|
| 3.1 | **六种剧本方法** | 从零试跑 **0/6** 完成全部交接。四路停在分场或引文检查，两路进导演但结尾单镜超 15 秒。代码未提交 | 同一原文、同一 seed 下 ≥4/6 走通全部交接；先提交代码 |
| 3.2 | 模型复核的漏判 | 流程样本被判通过，但仍有"拉头发不符合短发造型""三天跋涉表达不足""心理判断写进说明" | 复核规则针对这三类具体缺陷加检查 |
| 3.3 | **审查盲区普查** | 无台词镜头编造人声这个缺陷，审查体系覆盖率曾经是 0——不是漏检，是从没测过 | 列出所有"没有参考就不检查"的路径，逐条确认是设计还是盲区 |

## 持续项 — 备份与运维

| # | 事项 | 现状 |
|---|---|---|
| 4.1 | 诸天 ModelScope 补传 | 线上 `Flame4pd/zhutian-wanxianglu-episodes` 停在 09-08 的 993 集；本地已有 2379 个成片 |
| 4.2 | 465 个脱钩导出硬链接 | 雾月 372 + 星海 93，指向静音修复前的旧成片，占 30.9 GB。用该目录再传会传旧版 |
| 4.3 | 星海 ModelScope 覆盖 | 只导出了 1193/1600 集 |
| 4.4 | 上传记录不统一 | 雾月有 `modelscope_upload.json`，星海诸天没有；诸天走项目外的 `ms_stage/` + `tmp/ms_upload_once.py` |
| 4.5 | gpu52 5 号卡散热 | 空载 86°C 触发降频，09-10 的报告就建议过排查 |

## 需要你定的三件事

1. **诸天恢复生产的时机** —— 现在 paused。P0 不做就恢复，会在长片路由上返工。
2. **剧本影子门转不转正式门** —— 转，雾月从 100% 掉到 63%；不转，已发布内容带着幻觉人物。
3. **口型存量 12022 段重不重做** —— 这是本路线图里最贵的一项，要先估算 GPU 成本再决定。

## 不在本路线图内

- 磁盘：本轮已释放根分区 187 GB、disk0 515 GB、disk1 193 GB，不再是约束。
- 架构重构：已完成，见 [当前架构](architecture.md)。
- H3 无台词镜头编造人声：已上线，100% → 5.5%，707 集已回传。
